import { ConfigButton } from "../Config/ConfigButton";
import { IconButton } from "./IconButton";

export class RowOptionsButton extends IconButton {
    constructor(config: ConfigButton) {
        super(config);
    }
}